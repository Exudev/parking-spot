{
  "compilerOptions": {
    /* Projects */
    // Incremental compilation can be added if needed.

    /* Language and Environment */
    "target": "es2017",                                  /* Updated to support modern JavaScript features while maintaining compatibility. */
    "module": "commonjs",                                /* CommonJS is suitable for Node.js projects. */
    "strict": true,                                      /* Enables all strict type-checking options. */
    "esModuleInterop": true,                             /* Required for better compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true,            /* Ensures consistent file naming conventions. */
    "skipLibCheck": true,                                /* Skips checking declaration files for faster compilation. */
    "outDir": "./core/dist",                             /* Specifies output directory for compiled files. */
    "noImplicitAny": true,                               /* Ensures all variables are properly typed. */
    "strictFunctionTypes": true,                         /* Enforces stricter function type compatibility checks. */
    "strictBindCallApply": true,                         /* Validates the arguments for bind, call, and apply methods. */
    "noImplicitThis": true,                              /* Prevents usage of `this` with type `any`. */
    "alwaysStrict": true,                                /* Forces strict mode in every file. */

    /* Emit */
    "resolveJsonModule": true,                           /* Allows importing JSON files. */
    "removeComments": true,                              /* Removes comments from emitted JavaScript files. */

    /* Type Checking */
    // "noUnusedLocals": true,                              /* Reports unused local variables. */
    // "noUnusedParameters": true,                          /* Reports unused function parameters. */
    "strictPropertyInitialization": true,                /* Ensures class properties are initialized correctly. */
    "exactOptionalPropertyTypes": true,                  /* Keeps optional properties as defined. */

    /* Modules */
    "baseUrl": "./",                                     /* Base directory for module resolution. */
    "paths": {                                           /* Path mapping for cleaner imports. */
      "@src/*": ["src/*"]
    }
  },
  "include": ["src/", "test"],                  /* Fixed typo and included the correct directories. */
  "exclude": ["node_modules"]                            /* Excludes the node_modules directory. */
}